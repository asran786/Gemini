
import './spinner.css'; // Ensure to create this CSS file with the provided CSS

const Spinner = () => {
    return (
        <div className="spinner-container">
            <div className="spinner"></div>
        </div>
    );
};

export default Spinner;
